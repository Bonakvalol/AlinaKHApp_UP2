<Window x:Class="AlinaKHApp.CompleteAdWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlinaKHApp"
        mc:Ignorable="d"
        Title="CompleteAdWindow" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleText" Grid.Row="0" 
                   Text="Завершить объявление" 
                   FontSize="16" 
                   Margin="0,0,0,10"/>

        <TextBlock x:Name="CurrentPriceText" Grid.Row="1" 
                   Margin="0,0,0,15"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Финальная цена:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"/>
            <TextBox x:Name="FinalPriceTextBox" 
                     Width="100" 
                     VerticalAlignment="Center"/>
            <TextBlock Text="руб." 
                       VerticalAlignment="Center" 
                       Margin="5,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Завершить" 
                    Width="80" 
                    Margin="0,0,10,0" 
                    Click="CompleteButton_Click"/>
            <Button Content="Отмена" 
                    Width="80" 
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
