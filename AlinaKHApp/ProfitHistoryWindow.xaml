<Window x:Class="AlinaKHApp.ProfitHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlinaKHApp"
        mc:Ignorable="d"
        Title="ProfitHistoryWindow" Height="450" Width="800"
        Loaded="Window_Loaded">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Border Grid.Row="0" 
                Background="#4CAF50" 
                CornerRadius="10" 
                Padding="20"
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="ОБЩАЯ ПРИБЫЛЬ" 
                           Foreground="White" 
                           FontSize="14"
                           Opacity="0.9"/>
                <TextBlock x:Name="TotalProfitText" 
                           Text="0 руб." 
                           Foreground="White" 
                           FontSize="36"
                           FontWeight="Bold"/>
                <TextBlock x:Name="CompletedCountText" 
                           Text="Завершено сделок: 0" 
                           Foreground="White" 
                           FontSize="12"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" 
                BorderBrush="#E0E0E0" 
                BorderThickness="1"
                CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" 
                        Background="#F5F5F5" 
                        Padding="10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Объявление" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="Дата завершения" FontWeight="Bold"/>
                        <TextBlock Grid.Column="2" Text="Сумма сделки" FontWeight="Bold"/>
                        <TextBlock Grid.Column="3" Text="Прибыль" FontWeight="Bold"/>
                    </Grid>
                </Border>

                
                <ListView x:Name="ProfitListView" 
                          Grid.Row="1" 
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding AdTitle}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding CompletionDate, StringFormat={}{0:dd.MM.yyyy HH:mm}}"/>
                                    <TextBlock Grid.Column="2" Text="{Binding FinalPrice, StringFormat={}{0} руб.}"/>
                                    <TextBlock Grid.Column="3" 
                                               Text="{Binding Profit, StringFormat=+{0} руб.}" 
                                               Foreground="#4CAF50"
                                               FontWeight="Bold"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Экспорт в Excel" 
                    Width="120" Height="35"
                    Margin="0,0,10,0"/>
            <Button Content="Закрыть" 
                    Width="80" Height="35"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
